# Server
PORT=3000
DATABASE_URL=postgres://recover:recover@localhost:5432/recover

# AI assistant (provider-agnostic). Pick one provider; leave others blank.
AI_PROVIDER=claude            # claude | gemini | local
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
LOCAL_AI_BASE_URL=            # e.g. http://localhost:11434/v1 for an OpenAI-compatible local server
